# ── NestJS backend — development ─────────────────────────────────────────────
# Local machine: PostgreSQL running directly on the host.

PORT=4000
CORS_ORIGIN=http://localhost:3000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=signalscope
DB_USER=signalscope
DB_PASS=signalscope

JWT_SECRET=signal-scope-dev-jwt-secret-change-in-production
FRONTEND_URL=http://localhost:3000
API_PUBLIC_URL=http://localhost:4000
